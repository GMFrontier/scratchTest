export interface Balance {
  // user_id: string;
  currency: string;
  // description: string;
  balance: number;
  spent: number;
  totalCredit: number;
  dateEndPeriod: string;
  // creation_date: string;
}